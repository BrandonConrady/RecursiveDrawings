package recursion.trees;

public interface TreeConstants {
    //drawing mode, both draws leaves on both sides each iteration, ALTERNATE goes left right (LR) or right left (RL) each iteration
    int BOTH = 0, ALTERNATE_RL = 1, ALTERNATE_LR = 2;
}
